{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install && cd ../client && rm -rf node_modules && npm install && npm run build && cd ../server && npm run build"
  },
  "deploy": {
    "startCommand": "node server/dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
